{{- /* DPI sizes are baked as literals (shared with eww.scss.tmpl via the
       .chezmoitemplates/eww-sizes partial). eww 0.5.0 cannot resolve variables
       inside a window :geometry and GTK will not size empty boxes from runtime
       CSS variables, so nothing here relies on `eww update` for geometry. */ -}}
(include "./components/common.yuck")
(include "./components/bar.yuck")

(include "./components/popup-scrim.yuck")

(include "./components/profile-card.yuck")

(include "./components/network-popup.yuck")

(include "./components/control-center.yuck")

(include "./components/calendar-popup.yuck")

(include "./components/notification-popup.yuck")

(include "./components/osd.yuck")

(include "./components/updates-popup.yuck")

